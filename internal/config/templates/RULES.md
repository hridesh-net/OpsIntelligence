# Owner rule set

This file is **owner-only**: edit it on the machine (or via your editor), not through the agent.

The agent can **read** this file to follow your rules but **cannot** modify it via tools.

## Your rules

(Add concrete rules: tool usage, secrets, network, coding standards, etc.)

Optional: place additional owner-controlled files under **`policies/`** in this directory; the agent cannot change those either.
